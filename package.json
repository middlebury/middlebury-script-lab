{
  "scripts": {
    "start": "gulp dev",
    "build": "gulp build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d _site -b site"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "@middlebury/gulp-config": "^0.3.0",
    "gh-pages": "^3.0.0",
    "prettier": "^1.5.3"
  },
  "dependencies": {
    "headroom.js": "^0.9.4",
    "normalize.css": "^7.0.0",
    "onscrolling": "^0.3.3"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "none"
  }
}
