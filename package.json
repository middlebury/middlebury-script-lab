{
  "scripts": {
    "start": "gulp dev",
    "build": "gulp build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d _site -b site"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "@middlebury/gulp-config": "^1.0.0",
    "gh-pages": "^3.0.0",
    "prettier": "^1.5.3"
  },
  "dependencies": {
    "headroom.js": "^0.12.0",
    "normalize.css": "^7.0.0",
    "onscrolling": "^0.3.3"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "none"
  }
}
